<!DOCTYPE html>
<html lang="de">
<head>
    <meta charset="utf-8">
    <title>Plugin Dokumentation</title>
    <meta name="viewport" content="width=device-width, initial-scale=1">

    <style>
        body {
            font-family: system-ui, sans-serif;
            margin: 3rem;
            background: #fafafa;
        }
        .plugins {
            display: grid;
            grid-template-columns: repeat(auto-fill, minmax(320px, 1fr));
            gap: 1rem;
            margin-top: 2rem;
        }
        .plugin {
            background: white;
            padding: 1.25rem;
            border-radius: 8px;
            box-shadow: 0 2px 6px rgba(0,0,0,.05);
        }
        .meta {
            font-size: .85rem;
            color: #666;
        }
        .actions a {
            display: inline-block;
            margin-right: .5rem;
            padding: .4rem .7rem;
            font-size: .85rem;
            border-radius: 4px;
            text-decoration: none;
        }
        .primary {
            background: #005fcc;
            color: white;
        }
        .secondary {
            background: #e0e0e0;
            color: #222;
        }
    </style>
</head>

<body>

<h1>Plugin-Dokumentation</h1>
<p>Übersicht aller verfügbaren Plugins.</p>

<div class="plugins">
    {% for plugin in plugins %}
        <div class="plugin">
            <h2>{{ plugin.name }}</h2>
            <div class="meta">
                Latest: {{ plugin.latest }}<br>
                Aktualisiert: {{ plugin.updatedAt|date('Y-m-d') }}
            </div>
            <div class="actions">
                <a class="primary" href="/{{ plugin.slug }}/{{ plugin.latest }}/">Latest</a>
                <a class="secondary" href="/{{ plugin.slug }}/">Alle Versionen</a>
            </div>
        </div>
    {% endfor %}
</div>

<footer>
    Generiert am {{ generatedAt|date('Y-m-d H:i:s') }}
</footer>

</body>
</html>
