<!DOCTYPE html>
<html lang="de">
<head>
    <meta charset="utf-8">
    <title>Dokumentationen</title>
    <meta name="viewport" content="width=device-width, initial-scale=1">

    <link rel="stylesheet" href="./_assets/style.css">
</head>

<body>

<h1>Dokumentationen</h1>
<p>&Uuml;bersicht aller verf&uuml;gbaren Module.</p>

<div class="plugins">
    {% for plugin in plugins %}
        <div class="plugin">
            <h2>{{ plugin.name }}</h2>
            <div class="meta">
                Aktuellste: {{ plugin.latest }}<br>
                Aktualisiert: {{ plugin.updatedAt|date('Y-m-d') }}
            </div>
            <div class="actions">
                <a class="primary" href="./{{ plugin.slug }}/latest/" title="Weiterleitung zur aktuellsten Version">Aktuellste Version</a>
                <a class="secondary" href="./{{ plugin.slug }}/">Alle Versionen</a>
            </div>
        </div>
    {% endfor %}
</div>

<footer>
    Generiert am {{ generatedAt|date('Y-m-d H:i:s') }}
</footer>

</body>
</html>
